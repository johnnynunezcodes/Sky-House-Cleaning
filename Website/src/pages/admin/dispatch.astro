---
// This page was renamed to /admin/jobs (see AGENTS.md → "Jobs (formerly
// Dispatcher)" for why) — kept here as a redirect stub, not deleted,
// so any existing bookmark or saved link to /admin/dispatch still lands
// somewhere useful instead of 404ing.
export const prerender = false;
return Astro.redirect("/admin/jobs", 301);
---
