---
// Superseded by the Requests/Quotes split — see AGENTS.md → "Requests &
// Quotes". This combined board is gone; redirect any old bookmarks/links to
// the new Requests inbox rather than 404ing. Same "can't delete, only
// overwrite" pattern used for create-quote-job.js and the old dispatch.astro.
export const prerender = false;
return Astro.redirect("/admin/crm/requests", 301);
---
